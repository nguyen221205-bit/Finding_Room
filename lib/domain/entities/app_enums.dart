enum UserRole { user, landlord, admin }

enum ActiveUserMode { renter, landlord }

enum LandlordRequestStatus { pending, approved, rejected }

enum RoomStatus { pending, approved, rejected }

enum RoomAvailability { available, rented }
